  
  
  
  useEffect(() => {

    const listen=Appearance.getColorScheme()
  // const listen = Appearance.addChangeListener((colrTheme) => {
  //   if (colrTheme.colorScheme === 'dark') {
  //     setTheme('DARK');
  //   } else {
  //     setTheme('LIGHT');
  //   }
  // });

    console.log(listen)
    return () => {
      listen
    };
  }, []);